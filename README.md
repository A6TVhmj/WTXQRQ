# WTXQRQ
$\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$ uest is A tool of transfering files from adb

翻译： 有线传输加快快速远程任务 是一个从adb传输文件的工具

# How $\lvert 2-4 \rvert$ get it

Download the latest version of WtxQrqForever.rar from the release on the right

After decompression, open the file $\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$ uest.exe

# 获取方法：

从右边的 release 下载版本最新的 **WtxQrqForever.rar**

解压后打开里面的 $\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$ uest.exe

# How $\sqrt[2]{4}$ get a NetEase Cloud Music ID for a song

# 如何获得一首歌的网易云音乐 ID

在网易云音乐搜索找到你要找到歌（可能要登录账号）

找到网址栏有一段 id=xxxx 那一串数字就是 这首歌的网易云音乐 ID

# $\sqrt{2^4}$

# 举个例子

不眠之夜 网址是https://music.163.com/#/song?id=2122308127，ID就是2122308127。
