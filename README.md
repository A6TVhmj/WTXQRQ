# WTXQRQ
$\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$ uest is A tool of transfering files from adb

# How $\lvert 2-4 \rvert$ get it

Download the latest version of WtxQrqForever.rar from the release on the right

After decompression, open the file $\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$.exe

# 获取方法：

从右边的 release 下载版本最新的 **WtxQrqForever.rar**

解压后打开里面的 $\huge W$ ired_ $\huge T$ ransfer_e $\huge X$ pedite_ $\huge Q$ uick_ $\huge R$ emote_ $\huge Q$.exe
